name: Deploy Backend to KT Cloud

on:
  push:
    branches:
      - main
    paths:
      - 'backend/**'
      - 'docker-compose-external-mysql.yml'
      - '.github/workflows/deploy-backend.yml'

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Backend Docker Image
        working-directory: ./backend
        run: docker build -t quiz-backend:latest .

      - name: Save Docker Image
        run: docker save quiz-backend:latest | gzip > quiz-backend.tar.gz

      - name: Copy Docker Image to Server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.KT_SERVER_HOST }}
          username: ${{ secrets.KT_SERVER_USER }}
          port: ${{ secrets.KT_SSH_PORT || 22 }}
          password: ${{ secrets.KT_SERVER_PASSWORD }}
          source: "quiz-backend.tar.gz"
          target: "~/"
          timeout: 300s
          command_timeout: 30m

      - name: Copy backend source to Server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.KT_SERVER_HOST }}
          username: ${{ secrets.KT_SERVER_USER }}
          port: ${{ secrets.KT_SSH_PORT || 22 }}
          password: ${{ secrets.KT_SERVER_PASSWORD }}
          source: "backend/**"
          target: "~/simple-quiz-app/"
          strip_components: 0
          timeout: 300s

      - name: Copy docker-compose file to Server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.KT_SERVER_HOST }}
          username: ${{ secrets.KT_SERVER_USER }}
          port: ${{ secrets.KT_SSH_PORT || 22 }}
          password: ${{ secrets.KT_SERVER_PASSWORD }}
          source: "docker-compose-external-mysql.yml"
          target: "~/simple-quiz-app/"
          strip_components: 0
          timeout: 300s

      - name: Copy nginx directory to Server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.KT_SERVER_HOST }}
          username: ${{ secrets.KT_SERVER_USER }}
          port: ${{ secrets.KT_SSH_PORT || 22 }}
          password: ${{ secrets.KT_SERVER_PASSWORD }}
          source: "nginx/**"
          target: "~/simple-quiz-app/"
          strip_components: 0
          timeout: 300s

      - name: Deploy on KT Cloud Server
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.KT_SERVER_HOST }}
          username: ${{ secrets.KT_SERVER_USER }}
          port: ${{ secrets.KT_SSH_PORT || 22 }}
          password: ${{ secrets.KT_SERVER_PASSWORD }}
          timeout: 300s
          command_timeout: 30m
          envs: DB_HOST,DB_PORT,DB_NAME,DB_USERNAME,DB_PASSWORD,BACKEND_PORT,FRONTEND_PORT
          script: |
            cd ~/simple-quiz-app

            # .env 파일 생성
            cat > .env << EOF
            DB_HOST=${{ secrets.DB_HOST }}
            DB_PORT=${{ secrets.DB_PORT }}
            DB_NAME=${{ secrets.DB_NAME }}
            DB_USERNAME=${{ secrets.DB_USERNAME }}
            DB_PASSWORD=${{ secrets.DB_PASSWORD }}
            BACKEND_PORT=${{ secrets.BACKEND_PORT }}
            FRONTEND_PORT=${{ secrets.FRONTEND_PORT }}
            EOF

            # Docker 이미지 로드
            docker load < ~/quiz-backend.tar.gz
            rm ~/quiz-backend.tar.gz

            # 백엔드 및 nginx 컨테이너 재시작
            docker compose -f docker-compose-external-mysql.yml down backend nginx
            docker compose -f docker-compose-external-mysql.yml up -d backend nginx

            # 잠시 대기
            sleep 10

            # 로그 확인
            docker compose -f docker-compose-external-mysql.yml logs --tail=50 backend
            docker compose -f docker-compose-external-mysql.yml logs --tail=20 nginx

            # 상태 확인
            docker ps | grep -E 'quiz-backend|quiz-nginx'
